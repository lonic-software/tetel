# Grammar check — pass fixture

The same row with the unknown field removed.

```tetel
id: G-1
claim: The gateway validates every request header.
domain: src/gateway.rs#validate
extent: src/gateway.rs#validate
pin: abc123
kind: READING
status: VERIFIED
```
