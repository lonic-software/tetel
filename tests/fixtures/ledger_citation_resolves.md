# A memo whose prose cites its own ledger claim

Rows are against `src/foo.rs` at pin `abc1234` unless stated.

`foo` always returns 4, per the ledger [L-1]. This also cites a row that
was never defined anywhere [GHOST-1].

| ID | Proposition | Domain | Extent | Kind | Status |
|---|---|---|---|---|---|
| L-1 | `foo` always returns **4** | `foo`'s body | opened in full | READING | **OWED** |
