# A document with no evidence rows

This markdown file has ordinary prose and even an ordinary fenced code
block, but no ```tetel fence anywhere, so it is out of scope for `tetel
check` rather than clean.

```rust
fn main() {
    println!("not a tetel row");
}
```
