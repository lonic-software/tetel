# Subset check — fail fixture

Domain claims the whole file; extent only opened one symbol in it, so
domain is not a subset of extent.

```tetel
id: S-1
claim: Every UnboundedTicket call in the module respects the timeout.
domain: src/ticket.rs
extent: src/ticket.rs#new
pin: abc123
kind: READING
status: VERIFIED
```
